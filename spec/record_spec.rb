require 'spec_helper'

module MDM
   describe Record do
      before :each do
         @record = Record.new 
      end
      describe "#==" do
         it "returns false if other record is nil" do
            expect(
         end
      end
   end
end
